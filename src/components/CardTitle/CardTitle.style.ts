import { createUseStyles } from "react-jss";

export const useCardTitleStyles = createUseStyles({
  title: {
    fontSize: "24px",
    padding: "10px 16px",
    fontWeight: "bold",
    outline: 0,
    fontFamily: "Caveat, cursive",
  },
});
