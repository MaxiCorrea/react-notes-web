import { createUseStyles } from "react-jss";

export const useCardsStyles = createUseStyles({
  root: {
    display: "flex",
    justifyContent: "center",
    flexWrap: "wrap",
    margin: "0 auto",
  },
});
