import { createUseStyles } from "react-jss";

export const useCardBodyStyles = createUseStyles({
  body: {
    fontSize: "20px",
    padding: "10px 16px 16px",
    outline: 0,
    fontFamily: "Caveat, cursive",
  },
});
